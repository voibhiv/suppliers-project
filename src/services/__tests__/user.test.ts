
describe('User Test', () => {
  it("should return a error message when required values are empty and error 400", async() => {
    
    // const response = await global.testRequest.post('/beaches').send(newBeach);


    // // fields that are required
    // const requiredFields = ["name", "username", "password", "email", "cpf", "cnpj"];

    // let invalidFields = "";

    // for (const [key, value] of Object.entries(payload)) {
    //   if (value === "" && requiredFields.includes(key)) {
    //     invalidFields += ((invalidFields === "") ? `${key}` : `, ${key}`);
    //   }
    // }

    // const error = {
    //   code: 400,
    //   message: `Os campos ${invalidFields} são inválidos, por favor preencha-os e tente novamente`
    // }

    // expect(error).toEqual(error);


  });

  // it("should return a error message when name is empty", async() => {
  //   //
  // });

})